# Runtime dependencies mirrored from pyproject.toml ([project].dependencies).
# pyproject.toml remains the source of truth; keep the two in sync when adding
# or bumping a package. Install with `pip install -r requirements.txt`.
#
# For local Qwen3 via Ollama, no additional package is needed — the Ollama
# route uses langchain-openai's OpenAI-compatible client.

backtrader>=1.9.78.123
langchain-anthropic>=0.3.15
langchain-core>=0.3.81
langchain-experimental>=0.3.4
langchain-google-genai>=4.0.0
langchain-openai>=0.3.23
langgraph>=0.4.8
langgraph-checkpoint-sqlite>=2.0.0
pandas>=2.3.0
parsel>=1.10.0
python-dotenv>=1.0.0
pytz>=2025.2
questionary>=2.1.0
redis>=6.2.0
requests>=2.32.4
rich>=14.0.0
setuptools>=80.9.0
stockstats>=0.6.5
tqdm>=4.67.1
typer>=0.21.0
typing-extensions>=4.14.0
yfinance>=1.4.1

# Optional extras (not installed by default):
#   [dev]     : ruff, pytest, pytest-subtests  →  pip install -e ".[dev]"
#   [bedrock] : langchain-aws                  →  pip install ".[bedrock]"
