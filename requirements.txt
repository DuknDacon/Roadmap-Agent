# 공용 가상환경(../.venv, Roadmap-Agent 와 SeedUp 이 함께 씀)에 설치된 실제 버전으로 고정.
# pyproject.toml 의 dependencies 범위(>=,<) 안에서 검증된 스냅샷이며, 재현 가능한 로컬
# 설치/Docker 빌드용 버전 고정 목적이다. 의존성 자체를 바꿀 때는 pyproject.toml을 먼저
# 고치고, 새로 설치해 확인한 뒤 이 파일을 다시 생성한다.
google-genai==1.75.0
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1
llama-index-core==0.14.23
faiss-cpu==1.15.0
kiwipiepy==0.23.2
numpy==2.5.2
rank-bm25==0.2.2
