.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
.env
.env.*
!.env.example
data/raw/*.json
data/raw/.fss_finance_tips_cache/
data/fixtures/*.json
data/rag/
# Docker 이미지 파일 (scp 등으로 서버에 직접 전달, git에는 올리지 않음)
*.tar

# 대화 세션 저장소 (SQLite, 로컬 전용)
backend/app/.data/
*.sqlite
*.sqlite-wal
*.sqlite-shm
.mcp.json

# 공용 DB 마운트 지점 — 서버는 /srv/seedup/shared 를 쓰고 이미지에도 들어가지
# 않는다. 로컬에서 컨테이너를 띄우면 Docker가 여기에 파일을 만들 뿐이라
# 저장소에 올리지 않는다(DB 백업 파일이 커밋돼 checkout이 막힌 적 있음).
data/shared/
